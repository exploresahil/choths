const project = {
  name: "project",
  title: "Projects",
  type: "document",
  options: {
    singleton: true, // Define the document as a singleton
  },
  fields: [
    {
      name: "name",
      title: "Name",
      type: "string",
    },
  ],
};

export default project;
